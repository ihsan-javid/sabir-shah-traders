import CartClient from "./CartClient";

export const metadata = {
  title: "Shopping Cart — Sabir Shah Traders",
  robots: {
    index: false,
    follow: false,
  }
};

export default function CartPage() {
  return <CartClient />;
}
